package com.transformuk.hee.tis.profile.dto;

/**
 * Holds all the permission types we can have
 */
public enum PermissionType {
  INTERNAL, CONCERN, CONNECTION, NOTIFICATION, PROFILE, PCS, PP, REVALIDATION, REFERENCE, PERSON, ASSESSMENT, DOCUMENT;
}
